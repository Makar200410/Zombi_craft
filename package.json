{
  "name": "zombi-craft",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Voxel sandbox + village strategy + zombie waves + magic & research",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "three": "^0.186.1"
  },
  "devDependencies": {
    "vite": "^8.3.1"
  }
}
